import { PlaceholderPage, placeholderMetadata } from "@/components/layout/PlaceholderPage";

export const metadata = placeholderMetadata("Locations");

export default function Page() {
  return <PlaceholderPage title="Locations" />;
}
