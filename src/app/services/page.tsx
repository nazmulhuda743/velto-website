import { PlaceholderPage, placeholderMetadata } from "@/components/layout/PlaceholderPage";

export const metadata = placeholderMetadata("Services");

export default function Page() {
  return <PlaceholderPage title="Services" />;
}
