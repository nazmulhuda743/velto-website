import { PlaceholderPage, placeholderMetadata } from "@/components/layout/PlaceholderPage";

export const metadata = placeholderMetadata("Pricing");

export default function Page() {
  return <PlaceholderPage title="Pricing" />;
}
